import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u2.mcr',
  appName: 'ph-u2-mcr',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
